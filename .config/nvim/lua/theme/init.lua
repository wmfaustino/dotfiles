-- -----------------------------------------------------------------------------
-- => theme 
-- -----------------------------------------------------------------------------

-- require('utils')

utils.Options.set({
    { 'global', 't_Co', '256', {} },
    { 'global', 'termguicolors', true, {} },
    { 'global', 'background', 'dark', {} }
})

vim.cmd('colorscheme minimalist')
--" -----------------------------------------------------------------------------
